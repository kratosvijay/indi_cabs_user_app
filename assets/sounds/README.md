# Ride Rewards Notification Sound

Please place your cracker sound file (e.g., `cracker.mp3` or `cracker.wav`) in this directory.

Ensure the filename matches what is configured in:
1. `functions/src/index.ts` (for the FCM payload)
2. `pubspec.yaml` (for asset registration)

I have pre-registered `assets/sounds/cracker.mp3` in your `pubspec.yaml`.
